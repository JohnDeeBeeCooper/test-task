import React from 'react';
import { Helmet } from 'react-helmet';
import './style.css';

export default (props) => {
    return (
        <Helmet>
            <title>Products</title>
        </Helmet>
    );
}